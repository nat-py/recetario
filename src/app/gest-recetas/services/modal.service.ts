import { Injectable } from '@angular/core';
import { BehaviorSubject } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class ModalService {
/*
  private modalAparece = new BehaviorSubject<boolean>(false);
  public mostarModal = this.modalAparece.asObservable();

  showModal() {
    this.modalAparece.next(true);
  }

  closeModal() {
    this.modalAparece.next(false);
  } */
}

